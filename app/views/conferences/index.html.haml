%h1 Listing conferences

%table
  %tr
    %th Name
    %th Venue name
    %th Address
    %th Start date
    %th Days
    %th
    %th
    %th

  - @conferences.each do |conference|
    %tr
      %td= conference.name
      %td= conference.venue_name
      %td= conference.address
      %td= conference.start_date
      %td= conference.days
      %td= link_to 'Show', conference
      %td= link_to 'Edit', edit_conference_path(conference)
      %td= link_to 'Destroy', conference, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New conference', new_conference_path
